---
description: Database safety and Supabase RLS
alwaysApply: true
---

# DB safety

- Implement schema with least privilege.
- Use Supabase RLS for `assets` and `ai_insights` access paths.
- Prefer parameterized queries; never build SQL with untrusted strings.
- When adding columns, include migration notes and update queries/tests.

