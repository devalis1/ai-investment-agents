# Frontend (Next.js PWA)

This module will host the PWA dashboard:

- A form to add/manage tickers
- A portfolio view with metrics
- A results panel (`ai_insights`) with recommendations
- Partial offline behavior (depending on caching/PWA strategy)

Next step: initialize Next.js (App Router), integrate the Supabase client, and validate read/write.

